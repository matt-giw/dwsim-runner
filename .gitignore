# DWSIM release binaries — fetched by scripts/fetch-dwsim.sh, NEVER committed
# or redistributed (constitution: no conveyance of DWSIM binaries).
dwsim/

# .NET build output
bin/
obj/
publish/
*.user

# OS noise
.DS_Store

# User-authored templates persisted by /flowsheets/build-solve (runtime data,
# never committed; curated reference templates live in templates/)
user-templates/
